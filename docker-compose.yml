services:
  app:
    build: ./app
    container_name: realtime_app
    command: npm run start:dev
    ports:
      - "3000:3000"
    volumes:
      - ./app:/usr/src/app
      - ./clients:/usr/src/clients
